import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.course.ionicangular',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
